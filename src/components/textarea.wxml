<textarea
	hold-keyboard
	value="{{ value }}"
	style="{{ style }}"
	cursor-spacing="{{ 150 }}"
	placeholder="{{ placeholder }}"
	confirm-type="done"
	class="dx-textarea dx-class class"
	bind:keyboardheightchange="on_keyboard_height_change"
	bind:focus="on_focus"
	bind:blur="on_blur"
/>
