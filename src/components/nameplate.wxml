<view class="dx-nameplate class" style="{{ style }}">
	<view class="header" wx:if="{{ label }}">
		<slot name="icon" />

		<text class="label">{{ label }}</text>
	</view>

	<view class="loading" wx:if="{{ loading }}">
		<dx-loading />
	</view>

	<slot wx:else />
</view>
