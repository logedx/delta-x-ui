<view
	class="dx-input class"
	style="{{ style }}"
	wx:if="{{ !readonly && focus }}"
>
	<input
		focus
		hold-keyboard
		type="text"
		confirm-type="done"
		value="{{ value }}"
		cursor-spacing="{{ 150 }}"
		bind:blur="on_blur"
		bind:input="on_input"
		bind:confirm="on_confirm"
	/>
</view>

<view class="dx-input class" style="{{ style }}" bind:tap="on_focus" wx:else>
	<view class="icon" wx:if="{{ icon }}">
		<dx-icon size="var(--strong)" src="{{ icon }}" />
	</view>

	<text wx:if="{{ value }}">{{ value }}</text>
	<text class="placeholder" wx:else>{{ placeholder }}</text>
</view>
