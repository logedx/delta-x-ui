<view class="dx-input class" wx:if="{{ readonly }}">
	<text wx:if="{{ value }}">{{ value }}</text>

	<dx-indicator class="icon" icon="{{ icon }}" wx:if="{{ icon }}" />
</view>

<view class="dx-input class" wx:elif="{{ focus }}">
	<input
		focus
		hold-keyboard
		type="text"
		confirm-type="done"
		value="{{ value }}"
		cursor-spacing="{{ 150 }}"
		bind:blur="on_blur"
		bind:input="on_input"
		bind:confirm="on_confirm"
	/>
</view>

<view class="dx-input class" bind:tap="on_focus" wx:else>
	<text wx:if="{{ value }}">{{ value }}</text>
	<text class="placeholder" wx:else>{{ placeholder }}</text>

	<dx-indicator class="icon" icon="{{ icon }}" wx:if="{{ icon }}" />
</view>
