<input
	hold-keyboard
	value="{{ value }}"
	style="{{ style }}"
	cursor-spacing="{{ 150 }}"
	placeholder="{{ placeholder }}"
	type="text"
	confirm-type="done"
	class="dx-input dx-class class"
	bind:keyboardheightchange="on_keyboard_height_change"
	bind:focus="on_focus"
	bind:blur="on_blur"
/>
