<input
	hold-keyboard
	type="text"
	confirm-type="done"
	class="dx-input class"
	value="{{ value }}"
	style="{{ style }}"
	cursor-spacing="{{ 150 }}"
	placeholder="{{ placeholder }}"
	bind:keyboardheightchange="on_keyboard_height_change"
	bind:focus="on_focus"
	bind:blur="on_blur"
/>
