<view class="dx-fieldset class">
	<slot />

	<view class="ol">
		<dx-label
			title="{{ item[0] }}"
			divider="{{ index !== value.length - 1 }}"
			wx:key="0"
			wx:for="{{ value }}"
		>
			<text class="label">{{ item[1] }}</text>
		</dx-label>
	</view>
</view>
