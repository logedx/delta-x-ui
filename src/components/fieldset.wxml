<view class="dx-fieldset class">
	<slot />

	<view class="ol">
		<dx-label
			name="{{ item[0] }}"
			value="{{ item[1] }}"
			serif="{{ index < value.length - 1 }}"
			wx:key="0"
			wx:for="{{ value }}"
		/>
	</view>
</view>
