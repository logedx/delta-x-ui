<image
	fade-in
	mode="aspectFill"
	class="dx-avatar"
	src="{{ src }}"
	style="{{ style }}"
	bind:load="on_load"
	bind:error="on_error"
	wx:if="{{ src && !error }}"
/>

<view class="dx-avatar x" style="{{ style }}" wx:else />
